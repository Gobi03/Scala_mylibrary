:: , +:  // cons
isEmpty
==  // compare two lists
:+  // snoc
list(i)  // index i
last    // 最後の要素を取り出す
contains(x)  // xが要素に含まれているかどうか
::: , ++  // append
sorted  // sort(昇順)
sorted(Ordering[Int].reverse)  // sort(降順)
max, min  // 全要素の最大、最小

take(n) // 最初のn個の要素からなるリスト
takeRight(n)  // 最後のn個の要素からなるリスト
drop(n)  // 最初のn個の要素を除いたリスト
dropRight(n)  // 最後のn個の要素を除いたリスト
